# Frontend

Reserved for app code.

Note: the React Native app currently remains in the project root (`App.tsx`, `src/`, `android/`).
If needed later, we can move the app into this folder in a separate migration step.
